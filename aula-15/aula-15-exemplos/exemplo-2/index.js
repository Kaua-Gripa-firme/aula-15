
function compararNumeros (num1, num2) {
    if (num1 === num2) {
        console.log("Parabéns, os números são iguais")
    }
}

const numero1 = Number(prompt("Digite um número"))
const numero2 = Number(prompt("Confirme o número"))

compararNumeros(numero1, numero2)
